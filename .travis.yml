---
language: generic
dist: trusty

script:
  - cargo --version

matrix:
  include:
    - name: "build: linux x86_64"
      os: linux
      language: rust

    - name: "build: Windows x86_64"
      os: windows
      language: rust
